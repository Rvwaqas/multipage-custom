import styles from './AboutSection.module.css';

const AboutSection = () => {
  return (
    <section className={styles.about}>
      <h2>About Us</h2>
      <p>We are a team of professionals committed to providing exceptional services.</p>
    </section>
  );
};

export default AboutSection;
